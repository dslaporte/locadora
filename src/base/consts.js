const PRODUCT_NOT_FOUND = {
  message: 'Product not found!',
};

const INVALID_STOCK = {
  message: 'Quantity stock and quantity available must be equal',
};

const CUSTOMER_NOT_FOUND = {
  message: 'Customer not found!',
};

module.exports = {
  PRODUCT_NOT_FOUND,
  INVALID_STOCK,
  CUSTOMER_NOT_FOUND,
};
